#!/bin/bash

clear
figlet "GTX LINKSCAN" | lolcat
echo -e "\033[36mBu araç GtxAnqel tarafından geliştirilmiştir. Eğitim amaçlıdır.\033[0m"
echo "────────────────────────────────────────────────────"
echo ""

read -p "🔗 Analiz etmek istediğiniz URL'yi girin: " url

echo -e "\n[✓] Bağlantı kontrol ediliyor..."

# HTTPS kontrolü
if echo "$url" | grep -q "^https://"; then
    echo -e "\033[32m[+] HTTPS kullanılıyor: Güvenli bağlantı\033[0m"
else
    echo -e "\033[33m[-] HTTPS kullanılmıyor: Dikkatli olun\033[0m"
fi

# Domain WHOIS bilgisi çek (temel alan adı kontrolü)
domain=$(echo $url | awk -F/ '{print $3}')
whois_info=$(whois $domain | grep -i "Registrar\|Creation Date" | head -n 2)

echo -e "\n[+] Domain bilgisi:"
echo "$whois_info"

# URL çalışıyor mu kontrol et
httpcode=$(curl -s -o /dev/null -w "%{http_code}" "$url")

if [[ "$httpcode" == "200" ]]; then
    echo -e "\033[32m[✓] Bağlantı aktif.\033[0m"
else
    echo -e "\033[31m[✗] Bağlantı çalışmıyor veya şüpheli ($httpcode).\033[0m"
fi

# Virustotal API Entegrasyonu (isteğe bağlı – ekleyebilirim)

echo -e "\n\033[36mTarama tamamlandı. Bu sadece temel bir analizdir.\033[0m"